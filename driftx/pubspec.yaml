name: armyknife_driftx
description: Utilities for Drift library.
version: 1.0.0
resolution: workspace
repository: https://github.com/eaglesakura/flutter_armyknife/

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0
  drift: ^2.27.0
  drift_flutter: ^0.2.4
  rxdart: ^0.28.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0

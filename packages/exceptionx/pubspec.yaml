name: exceptionx
description: A starting point for Dart libraries or applications.
version: 2025.1.1
resolution: workspace
repository: https://github.com/eaglesakura/flutter_armyknife/exceptionx

environment:
  sdk: ">=3.8.0 <4.0.0"

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
dependencies:
  collection: ^1.19.0
  runtime_assert: ^0.0.1+2
